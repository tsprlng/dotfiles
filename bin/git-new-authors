#!/bin/zsh -e

mailmap="$(git config --get mailmap.file)"

git log --all --pretty='%ce %cn%n%ae %an'  \
	| sort | uniq \
	| while read email name; do
		grep -F -q "$email" "$mailmap" || echo "$name <$email>"
	done
