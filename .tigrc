bind main D @git meldshow %(commit)  # launch an external whole-directory diff of a commit's changes
set mailmap = true

set reference-format = hide:other
	# mostly to hide jj/keep refs

color cursor black green
